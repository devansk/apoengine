
2025-08-15 18:20 — Wersja: 0.12
- zooptymalizowano % dropów
- utworzono plik changelog
- poprawiono zwracany wynik z walki
- poprawiono czytelnosc kodu
- poprawiono ogólne błedy
- zooptymalizowano kod
- wdrożono walidację zdrowia do min 0 hp zamiast np -3 hp

Poprzednie zmiany (podsumowanie funkcjonalne)
- Inicjalizacja projektu i szkielet gry.
- System zapisu/odczytu gracza (saves/player_save.json).
- Model gracza: imię, zdrowie, doświadczenie, konto.
- System potworów z poziomami i podstawowymi statystykami.
- Prosty system walki turowej z logowaniem rund.
- System dropów i ekwipunku (dodawanie przedmiotów do inventory).
- Mechanika nagród: przyznawanie XP i środków po zwycięstwach.
- Logowanie zdarzeń do pliku saves/logs w ustandaryzowanym formacie.



'''
    CO DZIAŁA?
        -> klasa gracz
            - obsługa wczytywania i zapisywania stanu gracza
            - możliwość dodawania przedmiotów do ekwipunku
            - level up postaci
            - rozwoj statystyk gracza
        
        -> klasa przedmiot
            - obsługa przedmiotów, wczytywanie z pliku
            - możliwość dodawania przedmiotów do ekwipunku gracza
        
        -> klasa ekwipunek
            - obsługa ekwipunku, wczytywanie i zapisywanie stanu ekwipunku
            - możliwość wyświetlania ekwipunku gracza
            - możliwość dodawania przedmiotów do ekwipunku gracza
            - obsługa ilości przedmiotów w ekwipunku (np. 2x mikstura zdrowia)       
'''
'''
    CO ROBIE?
        -> dodać możliwość zapisu 3 postaci gracza
        -> dodać możliwość wyboru postaci gracza
        -> dodać 2 dodatkowe ekwipunki dla 2 pozostałych postaci
        -> klasa monster [80%]
        -> klasa walka [60%]
        -> klasa mapa
        -> klasa quest
        -> klasa sklep


'''

# Możliwe błędy i TODO — możliwości rozbudowy i optymalizacji
- Błędy do sprawdzenia:
  - walidacja i clamp zdrowia: upewnić się, że wszędzie jest min 0 (uniknięcie -4 w logach),
  - duplikaty zapisu/logowania przy szybkim powtarzaniu akcji (race condition),
  - spójność stanu ekwipunku przy zwiększaniu ilości (stacking),
  - poprawne zwiększanie/utrzymywanie stanu konta (overflow, wielokrotne dodania),
  - obsługa błędów I/O (brak pliku, uszkodzony JSON) — powinien być fallback i komunikat,
  - deterministyczność RNG dla testów (możliwe niestabilne wyniki w jednostkowych).
- Priorytetowe poprawki:
  - dodać jednostkowe testy dla mechaniki walki, dropów i zapisu/odczytu,
  - zabezpieczyć zapis pliku przez lock/atomic write (tmp->rename),
  - ujednolicić format logów i lokalizację komunikatów (i18n).
- Optymalizacje / refaktoring:
  - rozdzielić moduły: player, monster, fight, inventory, io, ui,
  - dodać typowanie (mypy) i linting (flake8/ruff) dla lepszej jakości kodu,
  - profilowanie krytycznych ścieżek (drop table, pętle AI) i cache'owanie wyników,
  - konfigurowalny system dropów (pliki JSON/yaml) zamiast hardcode.
- Funkcje do rozbudowy:
  - wieloosobowe zapisy postaci (3 postaci) i wybór postaci w menu,
  - pełna implementacja klasy map/quest/shop,
  - rozbudowana klasa walka: statusy, efekty czasowe, umiejętności,
  - GUI/cli poprawki: lepsze komunikaty, potwierdzenia, undo dla stash/changes,
  - wersjonowanie save'ów i migracje struktury save (schema version).
- Dla utrzymania projektu:
  - wdrożyć CI z testami i lintem,
  - dodać CHANGELOG zgodny z "Keep a Changelog" i semver,
  - przygotować dokumentację API/modułów oraz przykładowe